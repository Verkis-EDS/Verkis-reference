# TOOL_REGISTRY — `<project-slug>`

> Copyright © Verkís internal documentation. Every tool, library, daemon, or service this
> project depends on. Secrets column records *where* the secret lives, never the value.

| Tool | Type | Purpose | Location | Owner | Config | Secrets | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| `<name>` | `cli / lib / daemon / service` | `<purpose>` | `<host / path / URL>` | `<owner>` | `<config file>` | `<vault path>` or `none` | `active` / `planned` / `retired` | `<notes>` |
